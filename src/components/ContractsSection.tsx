// Stubbed after migration away from Convex. Content managed via dedicated
// endpoints (/api/contracts, /api/experts/me). This component is intentionally
// inert to keep legacy imports compiling.
export function ContractsSection() {
  return null;
}
export default ContractsSection;
